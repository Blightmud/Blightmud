#[allow(dead_code)]
pub enum Event {
    Prompt,
    ServerSend(Vec<u8>),
    ServerInput(String, bool),
    MudOutput(String),
    Output(String),
    Error(String),
    Info(String),
    UserInputBuffer(String),
    Connect(String, u32),
    Connected,
    ProtoEnabled(u8),
    GMCPReceive(String),
    GMCPRegister(String),
    AddTimedEvent(chrono::Duration, Option<u32>, u32),
    TimedEvent(u32),
    DropTimedEvent(u32),
    LoadScript(String),
    ResetScript,
    ScrollUp,
    ScrollDown,
    ScrollBottom,
    Disconnect,
    Redraw,
    Quit,
}
